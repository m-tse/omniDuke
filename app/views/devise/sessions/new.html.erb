<h2 class="title_of_horizontal_form">Sign in</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class=>"form-horizontal"} ) do |f| %>
  <%= render partial: "devise/email_password_form", locals:{f:f, submit_label:"Sign in"} %>

    <% end %>


