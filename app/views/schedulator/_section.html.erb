<% if !current_or_guest_user.current_schedulator.sections.include?(section) %>
    <%= render partial: "schedulator/addSection",
        locals: { section: section }
    %> 
<% else %> 
    <%= render partial: "schedulator/removeSection",
        locals: { section: section }
    %> 
<% end %>