<!-- REQUIRES locals: {courses: @courses} -->
<% courses.each do |course| %>
    <div class="course" id="course-<%= course.id %>">
    <% if current_or_guest_user.courses.include?(course) %>
        <%= render partial: "schedulator/side_remove_course", locals: {course: course} %>
    <% else %>
        <%= render partial: "schedulator/side_add_course", locals: {course: course} %>
    <% end %>
    </div>
<% end %>