<header>
  <div class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
      <div class="container">
	
	<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
	  <span class="icon-bar"></span>
	  <span class="icon-bar"></span>
	  <span class="icon-bar"></span>
	</a>
	<%= link_to "OmniDUKE", root_path, :class=>"brand" %>
    <div class="nav-collapse collapse">
		<ul class="nav pull-right">
            <li class=<%=request.fullpath=="/" || request.fullpath=="/home" ? "active" : ""%>>
	        <%= link_to( root_path) do -%><i class="icon-home icon-white"></i> Home<% end -%></li>
	        <li><%= link_to "Schedulator", schedulator_index_path%></li>
	        <% if !user_signed_in? %>
	            <li><%= link_to "Sign In", new_user_session_path %></li>

	    	<% else %>
	    		<li class=<%=request.fullpath.start_with?("/users") ? "active" : ""%>><%= link_to "Account Settings", edit_user_registration_path%></li>
	    		<li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
	    	<% end %>
	    
	    
	  </ul>
        </div>
	
      </div>
    </div>
  </div>
</header>
