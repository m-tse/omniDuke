<% for prereq in prerequisites %>
  <% if @subject == nil %>
    <%= link_to prereq.toDefaultCode, course_path(prereq) %>
  <% else %>
    <%= link_to prereq.toCode(@subject.id), course_path(prereq) %>
  <% end %>
<% end %>
  
