<!-- Requires locals: { course:@course } -->
        <%= link_to raw("<button class='btn btn-danger'>Remove</button>"), {
                controller: "bookbag_relationship",
                action: "destroy",
                id: getBookbagRelationshipId(current_or_guest_user, course),
                course: course.id
            }, {
                method: :delete,
                remote: true
            } 
        %>