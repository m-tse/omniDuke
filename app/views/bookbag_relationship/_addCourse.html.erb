<!-- Requires locals: { course:@course } -->
        <%= link_to raw("<button class='btn btn-primary'>Add</button>"), {
                controller: "bookbag_relationship",
                action: "create",
                course: course.id
            }, {
                method: :post,
                remote: true
            } 
        %>